<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body>
    <%- include("partials/header") %>
    <main>
      <p id="intro">
        Track your sprouts from seed to harvest 🌱 Create an account or log in
        to continue ☀️
      </p>
      <h2>Sign Up</h2>
      <form action="/signup" method="post" class="flex-container">
        <% if (message) {%>
        <p id="server-message"><%= message %></p>
        <% } %>
        <section>
          <label for="email">Email</label><br />
          <input
            id="email"
            name="email"
            type="email"
            autocomplete="email"
            required
            autofocus
          />
        </section>

        <section>
          <label for="username">Username</label><br />
          <input
            id="username"
            name="username"
            type="text"
            autocomplete="username"
            required
          />
        </section>

        <section>
          <label for="password">Password</label><br />
          <input
            id="new-password"
            name="password"
            type="password"
            autocomplete="new-password"
            required
          />
        </section>

        <section>
          <label for="confirm-password">Confirm Password</label><br />
          <input
            id="confirm"
            name="confirm"
            type="password"
            autocomplete="new-password"
            required
          />
        </section>
        <button type="submit" class="button">Submit</button>
        <p>Already have an account? <a href="/login">Log in</a></p>
      </form>
    </main>
  </body>
</html>
