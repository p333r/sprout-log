<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body>
    <%- include("partials/header") %>
    <main>
      <div id="intro">
        <p>
          Track your sprouts from seed to harvest, sign up or log in to continue.
        </p>
        <p>
          🌱☀️
        </p>
      </div>
      <h2>Login</h2>
        <form action="/login" method="post" class="flex-container">
          <% if (message) {%>
            <p id="server-message"> <%= message %> </p>
          <% } %>
          <div>
            <label for="username">Username</label><br>
            <input
              id="username"
              name="username"
              type="text"
              autocomplete="username"
              required
              autofocus
            />
          </div>

          <div>
            <label for="current-password">Password</label><br>
            <input
              id="current-password"
              name="password"
              type="password"
              autocomplete="current-password"
              required
            />
          </div>
          <button type="submit" class="button">Login</button>
          <p class="mb-0">Don't have an account? Log in as a guest or sign up.</p>
          <p>⬇️</p>   
        </form>

        <div id="guest-signup" class="flex-container gap-3">
          <a href="/login/guest" id="guest-btn" type="button" class="btn btn-warning">Guest</a>
          <a href="/signup" id="signup-btn" type="button" class="btn btn-warning">Sign up</a>
        </div>
        
        
      </div>
    </main>
  </body>
</html>
